/**
 * Constants for the boss timer system.
 */

// 5-minute reminder before boss spawn
const REMINDER_MINUTES_BEFORE = 5;

// GMT+8 timezone offset (Asia/Manila)
const TIMEZONE_OFFSET = 8;

module.exports = {
  REMINDER_MINUTES_BEFORE,
  TIMEZONE_OFFSET,
};
